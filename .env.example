# GitHub Personal Access Token for API access
# Required for indexing GitHub repositories
# Create at: https://github.com/settings/tokens
# Scope: public_repo (read-only for public repos)
#
# Without token: Rate limited to ~60 requests/hour
# With token: Rate limited to ~5,000 requests/hour
GITHUB_TOKEN=
